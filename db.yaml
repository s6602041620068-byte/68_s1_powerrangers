services:
  s1db:
    image: postgres:latest
    container_name: 68-s1-database
    ports:
      - ${POSTGRES_PART}:5432
    environment:
      POSTGRES_USER: ${POSTGRES_TUSER}
      POSTGRES_DB: ${POSTGRES_NAME}
      POSTGRES_PASSWORD: ${POSTGRES_PASS}